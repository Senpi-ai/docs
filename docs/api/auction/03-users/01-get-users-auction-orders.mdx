---
description: Learn how to use the Moxie Auction Subgraph API to get user's auction orders.
---

import InstallDependencies from "../../../../partial/_install_dependencies.mdx";
import PreRequisites from "../../../../partial/_pre_requisites.mdx";
import DeveloperSupport from "../../../../partial/_developer_support.mdx";
import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Get User's Auction Orders

In this tutorial, you will learn how to get the user's auction orders by calling the Moxie Auction Subgraph API.

<PreRequisites />

<InstallDependencies />

## Step 2: Construct The GraphQL Query

Then, you can get the user's auction orders by calling the subgraph API with the following query:

<Tabs>
<TabItem value="query" label="Query">

```graphql
query MyQuery($userAddress: Bytes!) {
  users(where: { address: $userAddress }) {
    orders {
      buyAmount
      sellAmount
      price
      status
      timestamp
      txHash
    }
  }
}
```

</TabItem>
<TabItem value="variable" label="Variable">

```json
{
  "userAddress": "0x1a45675bd474c8e3056a7dbd19e738d7849c0d26"
}
```

</TabItem>
<TabItem value="response" label="Response">

```json
{
  "data": {
    "users": [
      {
        "orders": [
          {
            "buyAmount": "50000000000000000000",
            "sellAmount": "100000000000000000000",
            "price": "2",
            "status": "Cancelled",
            "timestamp": "1721122810",
            "txHash": "0x0681eec905142e218f3feb45280f05164d3cfdc132ca59cc3bed05944ff60d9e"
          }
        ]
      }
    ]
  }
}
```

</TabItem>
</Tabs>

## Step 3: Add The API Query To Your Code

With this GraphQL query, you can add it to your source code and call the API with the `graphql-request` library:

<Tabs>
<TabItem value="grapqhl_ts" label="graphql-request (TS)">

```tsx
import { gql, GraphQLClient } from "graphql-request";

const query = gql`
  query MyQuery($userAddress: Bytes!) {
    users(where: { address: $userAddress }) {
      orders {
        buyAmount
        sellAmount
        price
        status
        timestamp
        txHash
      }
    }
  }
`;

const graphQLClient = new GraphQLClient(
  "https://api.studio.thegraph.com/proxy/27864/auction/v3.4.3"
);

try {
  const data = await graphQLClient.request(query, {
    userAddress: "0x1a45675bd474c8e3056a7dbd19e738d7849c0d26",
  });
  console.log(data);
} catch (e) {
  throw new Error(e);
}
```

</TabItem>
<TabItem value="graphql_js" label="graphql-request (JS)">

```jsx
const { gql, GraphQLClient } = require("graphql-request");

const query = gql`
  query MyQuery($userAddress: Bytes!) {
    users(where: { address: $userAddress }) {
      orders {
        buyAmount
        sellAmount
        price
        status
        timestamp
        txHash
      }
    }
  }
`;

const graphQLClient = new GraphQLClient(
  "https://api.studio.thegraph.com/proxy/27864/auction/v3.4.3"
);

try {
  const data = await graphQLClient.request(query, {
    userAddress: "0x1a45675bd474c8e3056a7dbd19e738d7849c0d26",
  });
  console.log(data);
} catch (e) {
  throw new Error(e);
}
```

</TabItem>
</Tabs>

<DeveloperSupport />
