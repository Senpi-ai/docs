---
description: Learn how to use the Moxie Protocol Subgraph API to get the user's buy orders.
---

import InstallDependencies from "../../../../partial/_install_dependencies.mdx";
import PreRequisites from "../../../../partial/_pre_requisites.mdx";
import DeveloperSupport from "../../../../partial/_developer_support.mdx";
import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Get User's Buy Orders

In this tutorial, you will learn how to get the user's buy orders on the Moxie protocol by calling the Moxie Protocol Subgraph API.

<PreRequisites />

<InstallDependencies />

## Step 2: Construct The GraphQL Query

Then, you can get the user's buy orders by calling the subgraph API with the following query:

<Tabs>
<TabItem value="query" label="Query">

```graphql
query MyQuery($userAddress: ID) {
  users(where: { id: $userAddress }) {
    buyOrders {
      price
      protocolToken
      protocolTokenAmount
      protocolFeeTransfer {
        txHash
        amount
      }
      subjectToken {
        name
        symbol
      }
      subjectAmount
      subjectFeeTransfer {
        txHash
        amount
      }
    }
  }
}
```

</TabItem>
<TabItem value="variable" label="Variable">

```json
{
  "userAddress": "0x20e04003c2edfbca86300f6b2e6736142718dae3"
}
```

</TabItem>
<TabItem value="response" label="Response">

```json
{
  "data": {
    "users": [
      {
        "buyOrders": [
          {
            "price": "0.5456425784420317897",
            "protocolToken": "0xf80945fc1436b0ae8b86c8835f09870deeaf03d5",
            "protocolTokenAmount": "500000000000000000000",
            "protocolFeeTransfer": {
              "txHash": "0x26edba6f0c967cd58780813f332dcaa83b4e5e119a08cb6b43f4fb0bd60b0abc",
              "amount": "5000000000000000000"
            },
            "subjectToken": {
              "name": "fid-3761",
              "symbol": "fid-3761"
            },
            "subjectAmount": "272821289221015894850",
            "subjectFeeTransfer": {
              "txHash": "0x26edba6f0c967cd58780813f332dcaa83b4e5e119a08cb6b43f4fb0bd60b0abc",
              "amount": "15000000000000000000"
            }
          }
        ]
      }
    ]
  }
}
```

</TabItem>
</Tabs>

## Step 3: Add The API Query To Your Code

With this GraphQL query, you can add it to your source code:

<Tabs>
<TabItem value="grapqhl_ts" label="graphql-request (TS)">

```tsx
import { gql, GraphQLClient } from "graphql-request";

const query = gql`
  query MyQuery($userAddress: ID) {
    users(where: { id: $userAddress }) {
      buyOrders {
        price
        protocolToken
        protocolTokenAmount
        protocolFeeTransfer {
          txHash
          amount
        }
        subjectToken {
          name
          symbol
        }
        subjectAmount
        subjectFeeTransfer {
          txHash
          amount
        }
      }
    }
  }
`;

const graphQLClient = new GraphQLClient(
  "https://api.studio.thegraph.com/proxy/27864/protocol/v3.1.9"
);

try {
  const data = await graphQLClient.request(query, {
    userAddress: "0x20e04003c2edfbca86300f6b2e6736142718dae3",
  });
  console.log(data);
} catch (e) {
  throw new Error(e);
}
```

</TabItem>
<TabItem value="graphql_js" label="graphql-request (JS)">

```jsx
const { gql, GraphQLClient } = require("graphql-request");

const query = gql`
  query MyQuery($userAddress: ID) {
    users(where: { id: $userAddress }) {
      buyOrders {
        price
        protocolToken
        protocolTokenAmount
        protocolFeeTransfer {
          txHash
          amount
        }
        subjectToken {
          name
          symbol
        }
        subjectAmount
        subjectFeeTransfer {
          txHash
          amount
        }
      }
    }
  }
`;

const graphQLClient = new GraphQLClient(
  "https://api.studio.thegraph.com/proxy/27864/protocol/v3.1.9"
);

try {
  const data = await graphQLClient.request(query, {
    userAddress: "0x20e04003c2edfbca86300f6b2e6736142718dae3",
  });
  console.log(data);
} catch (e) {
  throw new Error(e);
}
```

</TabItem>
</Tabs>

<DeveloperSupport />

## Next Steps
