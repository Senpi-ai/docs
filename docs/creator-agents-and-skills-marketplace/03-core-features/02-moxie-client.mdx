import DeveloperSupport from "../../../partial/_developer_support.mdx";

# Moxie Client

The Moxie Client injects several predefined states that can be directly used during skill development. These states include:

## States

### `agentName`

```ts
export default Action {
    handler: async (
    runtime: IAgentRuntime,
    message: Memory,
    state: State,
    _options: { [key: string]: unknown },
    callback?: HandlerCallback
    ): Promise<boolean> => {
        const agentName: string = state.agentName;
    }
}
```

### `moxieUserInfo`

```ts
export default Action {
    handler: async (
    runtime: IAgentRuntime,
    message: Memory,
    state: State,
    _options: { [key: string]: unknown },
    callback?: HandlerCallback
    ): Promise<boolean> => {
        const moxieUserInfo: MoxieUser = state.moxieUserInfo;
    }
}
```

### `agentWallet`

```ts
export default Action {
    handler: async (
    runtime: IAgentRuntime,
    message: Memory,
    state: State,
    _options: { [key: string]: unknown },
    callback?: HandlerCallback
    ): Promise<boolean> => {
        const agentWallet: Wallet = state.agentWallet;
    }
}
```

## More Resources

- [`states` References](../04-references/states/index.mdx)
  - [`agentName`](../04-references/states/agentName.mdx)
  - [`agentWallet`](../04-references/states/agentWallet.mdx)
  - [`moxieUserInfo`](../04-references/states/moxieUserInfo.mdx)

<DeveloperSupport />
