---
description: agentWallet state returns a class instance of Senpi agent's embedded wallet that can be used to execute transactions and sign messages.
---

# `agentWallet`

## Description

`agentWallet` state returns a class instance of Senpi agent's embedded wallet that can be used to execute transactions and sign messages.

## Example

```ts
const agentWallet: MoxieWalletClient = state.agentWallet;
```
